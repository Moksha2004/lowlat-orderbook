#include <gtest/gtest.h>
#include "orderbook/types.hpp"
// Day 1 deliverable: this file COMPILES and runs (trivially green).
// Day 2: replace with real tests for Order/Side/typedefs.
TEST(Scaffold, Compiles) { EXPECT_TRUE(true); }
